EC:
  allocator: 
    - LRU
    #- TinyLFU
    #- LRU2Q
  MLConfig:
    #- use_eviction_control,1,force_run,0,window_size_factor,1,batch_size_factor,1,debug_mode,0,async_mode,1,n_edc,0,n_deltas,0,num_threads,1,rpe_target,1,debug_mode,0,version,3,use_oracle,0,use_global_clock,1,num_iterations,16,num_leaves,16
    - use_eviction_control,1,force_run,1,debug_mode,0,window_size_factor,1,batch_size_factor,1,async_mode,1,n_edc,0,n_deltas,0,num_threads,1,rpe_target,1,debug_mode,0,version,2,use_oracle,0,use_global_clock,1,num_iterations,16,num_leaves,16
  useEvictionControl:
    - true
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    #- tail-age
    #- hits
    - random

LRU:
  allocator: LRU
  useEvictionControl:
    - false
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    - tail-age
    #- hits
    #- random

LRU2Q:
  allocator: LRU2Q
  useEvictionControl:
    - false
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    #- tail-age
    - random

TinyLFU:
  allocator: TinyLFU
  useEvictionControl:
    - false
  MLConfig:
    - admission_control,1
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    #- tail-age 
    - random
    #- hits