EC:
  allocator: 
    #- LRU
    - S3FIFO
    #- TinyLFU
    #- LRU2Q
  MLConfig:
    #- use_eviction_control,1,evict_all_mode,0,training_sample_rate,5,model_type,0,batch_size_factor,1,window_size_factor,1,debug_mode,0,num_iterations,16,num_leaves,16,rpe_target,1,window_cnt,32,version,1106
    #- use_eviction_control,1,evict_all_mode,0,training_sample_rate,1,model_type,0,batch_size_factor,1,window_size_factor,1,debug_mode,0,num_iterations,16,num_leaves,16,rpe_target,1,window_cnt,32,prediction_batch_size,128,version,1130,meta_update_ssd,0,bfRatio,0.01
    #- use_eviction_control,1,admission_control,1,evict_all_mode,0,training_sample_rate,1,model_type,0,batch_size_factor,1,window_size_factor,1,debug_mode,0,num_iterations,16,num_leaves,16,rpe_target,1,window_cnt,32,prediction_batch_size,128,version,bf,meta_update_ssd,0,bfRatio,0.01
    - use_eviction_control,1,evict_all_mode,0,training_sample_rate,1,model_type,0,batch_size_factor,1,window_size_factor,1,debug_mode,0,num_iterations,16,num_leaves,16,rpe_target,3,window_cnt,32,prediction_batch_size,64,version,0104,meta_update_ssd,0,bfRatio,0.01,heuristic_mode,12,heuristic_aided,4
    #- use_eviction_control,1,evict_all_mode,0,training_sample_rate,1,model_type,0,batch_size_factor,1,window_size_factor,1,debug_mode,0,num_iterations,16,num_leaves,16,rpe_target,1,window_cnt,32,prediction_batch_size,64,version,1130,meta_update_ssd,0,bfRatio,0.01,heuristic_mode,15,heuristic_aided,2
    - use_eviction_control,1,evict_all_mode,0,training_sample_rate,1,model_type,0,batch_size_factor,1,window_size_factor,1,debug_mode,0,num_iterations,16,num_leaves,16,rpe_target,0.5,window_cnt,32,prediction_batch_size,64,version,0104,meta_update_ssd,0,bfRatio,0.01,heuristic_mode,8,heuristic_aided,4
  useEvictionControl:
    - true
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    #- tail-age
    #- hits
    - random

LRU:
  allocator: LRU
  useEvictionControl:
    - false
  MLConfig:
    - admission_control,1,version,bf
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    - tail-age
    #- hits
    #- random

S3FIFO:
  allocator: S3FIFO
  useEvictionControl:
    - false
  MLConfig:
    - admission_control,1,version,bf
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    - tail-age
    #- hits
    #- random

LRU*:
  allocator: LRU
  useEvictionControl:
    - false
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    - tail-age
    #- hits
    #- random
  lruRefreshSec:
    - 0

FIFO:
  allocator: LRU
  useEvictionControl:
    - false
  poolRebalanceIntervalSec:
    - 0
  lruRefreshRatio:
    - 100

LRU2Q:
  allocator: LRU2Q
  useEvictionControl:
    - false
  MLConfig:
    - admission_control,1,version,bf
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    #- tail-age
    - random

TinyLFU:
  allocator: TinyLFU
  useEvictionControl:
    - false
  MLConfig:
    - admission_control,1,version,bf
  poolRebalanceIntervalSec:
    - 0
  rebalanceStrategy:
    #- tail-age 
    - random
    #- hits